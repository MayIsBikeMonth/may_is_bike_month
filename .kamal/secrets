# Secrets defined here are available for reference under registry/password, env/secret, builder/secrets,
# and accessories/*/env/secret in config/deploy.yml. All secrets should be pulled from either
# password manager, ENV, or a file. DO NOT ENTER RAW CREDENTIALS HERE! This file needs to be safe for git.

# When secrets are already set (CI), skip 1Password and gh CLI
KAMAL_REGISTRY_PASSWORD=${KAMAL_REGISTRY_PASSWORD:-$(gh auth token)}

if [ -z "$RAILS_MASTER_KEY" ]; then
  SECRETS=$(kamal secrets fetch --adapter 1password --account bike-index --from Kamal/MayIsBikeMonth RAILS_MASTER_KEY HONEYBADGER_API_KEY POSTGRES_PASSWORD STRAVA_CLIENT_ID STRAVA_SECRET DEVISE_SECRET_KEY DEVISE_PEPPER)
  RAILS_MASTER_KEY=$(kamal secrets extract RAILS_MASTER_KEY ${SECRETS})
  HONEYBADGER_API_KEY=$(kamal secrets extract HONEYBADGER_API_KEY ${SECRETS})
  POSTGRES_PASSWORD=$(kamal secrets extract POSTGRES_PASSWORD ${SECRETS})
  STRAVA_CLIENT_ID=$(kamal secrets extract STRAVA_CLIENT_ID ${SECRETS})
  STRAVA_SECRET=$(kamal secrets extract STRAVA_SECRET ${SECRETS})
  DEVISE_SECRET_KEY=$(kamal secrets extract DEVISE_SECRET_KEY ${SECRETS})
  DEVISE_PEPPER=$(kamal secrets extract DEVISE_PEPPER ${SECRETS})
fi
