# Secrets defined here are available for reference under registry/password, env/secret, builder/secrets,
# and accessories/*/env/secret in config/deploy.yml. All secrets should be pulled from either
# password manager, ENV, or a file. DO NOT ENTER RAW CREDENTIALS HERE! This file needs to be safe for git.

# Use GitHub token for GHCR authentication
KAMAL_REGISTRY_PASSWORD=$(gh auth token)

# Fetch secrets from 1Password
SECRETS=$(kamal secrets fetch --adapter 1password --account bike-index --from Kamal/MayIsBikeMonth RAILS_MASTER_KEY HONEYBADGER_API_KEY POSTGRESQL_PASSWORD)
RAILS_MASTER_KEY=$(kamal secrets extract RAILS_MASTER_KEY ${SECRETS})
HONEYBADGER_API_KEY=$(kamal secrets extract HONEYBADGER_API_KEY ${SECRETS})
# POSTGRESQL_PASSWORD is used by the Rails app; POSTGRES_PASSWORD is used by the Postgres container
POSTGRESQL_PASSWORD=$(kamal secrets extract POSTGRESQL_PASSWORD ${SECRETS})
POSTGRES_PASSWORD=$POSTGRESQL_PASSWORD
