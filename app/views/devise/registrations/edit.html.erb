<%= render(UI::Card::Component.new) do %>
  <%= render(UI::Header::Component.new(text: "Edit Email and Password")) %>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: {method: :put}, builder: MayIsBikeMonthFormBuilder) do |f| %>
    <%= render(UI::AlertForErrors::Component.new(object: resource)) %>

    <div class="mb-4">
      <%= f.label :password, "New password", class: Form::Group::Component::LABEL_CLASSES %>
      <%= f.password_field :password, autocomplete: "new-password", class: Form::Input::Component::INPUT_CLASSES %>
      <p class="text-xs mt-1 text-gray-500 dark:text-gray-400">
        <%= @minimum_password_length %> characters minimum
      </p>
    </div>

    <div class="mb-4">
      <%= f.label :password_confirmation, class: Form::Group::Component::LABEL_CLASSES %>
      <%= f.password_field :password_confirmation, autocomplete: "new-password", class: Form::Input::Component::INPUT_CLASSES %>
    </div>

    <div class="mb-4">
      <%= f.label :current_password, class: Form::Group::Component::LABEL_CLASSES %>
      <%= f.password_field :current_password, autocomplete: "current-password", class: Form::Input::Component::INPUT_CLASSES %>
    </div>

    <%= render(UI::Button::Component.new(text: "Update", color: :primary, kind: :submit)) %>
  <% end %>
<% end %>
